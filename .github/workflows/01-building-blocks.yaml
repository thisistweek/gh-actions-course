name: 01-Building-Blocks
on: [push, workflow_dispatch]

jobs:
    first-job:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "hello world"
    second-job:
        runs-on: ubuntu-latest
        steps:
            - name: Failed step
              run: |
                echo "I will fail"
                exit 1
            - name: Say goodbye
              run: echo "goodbye"
                